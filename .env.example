# CouchDB Configuration
COUCHDB_USER=admin
COUCHDB_PASSWORD=change-me-to-strong-password

# Caddy Reverse Proxy (SSL/HTTPS)
# Local testing: localhost
# VPS production: your-domain.com (must point DNS to VPS IP first)
VPS_DOMAIN=localhost

# Wireguard Configuration
VPS_IP=YOUR_VPS_IP_ADDRESS
WIREGUARD_PEERS=3
TZ=UTC
